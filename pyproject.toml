[project]
name = "gpt-builder-wizard"
version = "1.0.0"
description = "Custom GPT / Gem package builder for library search strategy assistants"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.35.0",
    "pyyaml>=6.0.1",
]
